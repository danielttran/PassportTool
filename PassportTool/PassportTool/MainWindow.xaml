<Window
    x:Class="PassportTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PassportTool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Padding="20" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,20">
                <TextBlock Text="Sheet Layout Configuration" Style="{StaticResource TitleTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="20">
                    <NumberBox x:Name="NbRows" Header="Rows" Value="2" Minimum="1" Maximum="10" SpinButtonPlacementMode="Inline" ValueChanged="OnSettingsChanged"/>
                    <NumberBox x:Name="NbCols" Header="Cols" Value="3" Minimum="1" Maximum="10" SpinButtonPlacementMode="Inline" ValueChanged="OnSettingsChanged"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Background="WhiteSmoke">
                <Viewbox Stretch="Uniform">
                    <Grid x:Name="PreviewGrid" Background="White" Width="600" Height="400">
                    </Grid>
                </Viewbox>
            </Border>
        </Grid>

        <StackPanel Grid.Column="1" Background="LightGray" Padding="20" Spacing="20" BorderBrush="Gray" BorderThickness="1,0,0,0">
            <TextBlock Text="IMAGE SOURCE" Style="{StaticResource SubtitleTextBlockStyle}"/>

            <Border BorderBrush="DarkGray" BorderThickness="2" Background="White" Height="300">
                <Image x:Name="SourceImagePreview" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <Button x:Name="BtnPickImage" Content="Choose Photo..." HorizontalAlignment="Stretch" Click="BtnPickImage_Click"/>
        </StackPanel>
    </Grid>
</Window>